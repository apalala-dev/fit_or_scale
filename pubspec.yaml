name: fit_or_scale
description: Scale down the wrapped widget if its available space is below minWidth or minHeight provided values.
version: 0.0.1
homepage: https://github.com/apalala-dev/fit_or_scale

environment:
  sdk: '>=2.17.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0